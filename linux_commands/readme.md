# pwd = print working directory
# ls = list directory (folder) contents
# cd = change directory
# touch = create an empty file.
# cat = display file contents
# mkdir = make directory (folder)
# rm -rf = remove files or directories forcefully and recursively (be careful when using this command. You can't undo it!)


/home/valkyrie/Desktop/Python_Playground/python_batch3/linux_commands

cd home
cd valkyrie
cd Desktop
cd Python_Playground
cd python_batch3
cd linux_commands
ls
touch example.txt
cd ..
cd ..
cd ..
cd Python_Playground

